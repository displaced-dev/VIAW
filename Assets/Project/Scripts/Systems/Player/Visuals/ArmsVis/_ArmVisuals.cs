using UnityEngine;

// Summary
// Script responsible for managing the animation components of player arms

namespace VIAW.Systems.Player
{
    public class _ArmVisuals : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
