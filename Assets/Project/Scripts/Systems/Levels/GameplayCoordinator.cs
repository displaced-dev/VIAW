using UnityEngine;

// Summary
// Single source of truth for how the game is playing out 
// Server / host owns this information with clients being directed accordingly

namespace VIAW.Systems.Level
{
    public class GameplayCoordinator : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
